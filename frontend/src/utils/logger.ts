// Development-only logging utility
const isDevelopment = false; // Disabled for production

export const logger = {
  log: (...args: unknown[]) => {
    // Logging disabled
  },
  warn: (...args: unknown[]) => {
    // Logging disabled
  },
  error: (...args: unknown[]) => {
    // Logging disabled
  }
};