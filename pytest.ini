[pytest]
# Keep unit test runs deterministic by only collecting from `tests/`.
# Many scripts under `testing/` are demos/integration checks and should not
# be collected by default.
testpaths = tests
python_files = test_*.py

markers =
    integration: marks tests as integration tests

asyncio_default_fixture_loop_scope = function
